import React from 'react'
import { LinkCon } from 'react-router-bootstrap'

const Paginate = ({ pages, page, isAdmin = false, keyword = '' }) => {
    return (
        <div>
            
        </div>
    )
}

export default Paginate
