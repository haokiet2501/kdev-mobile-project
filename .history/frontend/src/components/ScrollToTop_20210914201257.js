import React from 'react'
import { useEff }

const ScrollToTop = () => {
    const {pathname} = useLocation()

    return (
        <div>
            
        </div>
    )
}

export default ScrollToTop
