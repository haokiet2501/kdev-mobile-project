import React from 'react'
import { useEffect(() => {
    effect
    return () => {
        cleanup
    }
}, [input]) }

const ScrollToTop = () => {
    const {pathname} = useLocation()

    return (
        <div>
            
        </div>
    )
}

export default ScrollToTop
