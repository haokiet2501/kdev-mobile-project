import React from 'react'
import { user }

const ScrollToTop = () => {
    const {pathname} = useLocation()

    return (
        <div>
            
        </div>
    )
}

export default ScrollToTop
